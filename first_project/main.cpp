#include <iostream>
using namespace std;
int main()
{
  // skärm (cout), utmatningsoperator(<<),
  // text ("Hello Worls"),
  // utmatningsoperatorn(<<),
  // radbrytning(endl), slut på sats(;)
    cout << "Hello world!" << endl;
  //  hej vid fel i programmet
  // returneras ett heltal som inte är 0
  // till operativsystemet
    return 0; // om allt går bra returneras 0
}
